<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent"
              android:gravity="center"
              android:orientation="vertical">

    <!-- imagen definida en light_bulb.xml
        que está formada por dos imágenes distintas
       para definir el efecto de pasar de una imagen a otra
       en este caso bombilla apagada o encendida -->
    <ImageView
        android:id="@+id/image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/light_bulb">
    </ImageView>


    <!-- el aspecto del botón está definido en button.xml
         que usa una imagen entre varías (item) según el estado del ToggleButton -->
    <ToggleButton
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/button"
        android:paddingBottom="20dip"
        android:paddingLeft="40dip"
        android:paddingRight="40dip"
        android:paddingTop="20dip"
        android:text="@string/toggleButton"
        android:textAppearance="@android:style/TextAppearance.Large"
        android:textColor="@android:color/white"/>

</LinearLayout>